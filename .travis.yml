language: node_js
node_js:
  - 8
addons:
  apt:
    packages:
      - libgconf-2-4
cache:
  directories:
    - node_modules
    - ~/.cache
install:
  yarn
script:
  - yarn lint
  - yarn test:unit
  - yarn test:e2e --record